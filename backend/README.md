# Backend (data model)

Runtime API and auth are provided by **Base44**. This folder holds **entity JSON schemas** that describe the app’s data model (`backend/entities/*.json`). They are documentation/source-of-truth for fields used by the frontend SDK; they are not a runnable server.
